<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="top.zuishare.dao.NewsDao">

    <select id="getNewsList" resultType="top.zuishare.spi.model.News">
        select id,clicks,title,publishDate,keyWords from tb_news where publishDate is not null order by publishDate desc,id desc
    </select>
    
    <select id="loadNews" resultType="top.zuishare.spi.model.News" parameterType="int">
        select id,clicks,title,publishDate,keyWords,content,bref from tb_news where id=#{newsId} and publishDate is not null limit 1
    </select>

    <select id="countNews" resultType="long">
        select count(1) from tb_news where publishDate is not null limit 1
    </select>

</mapper>
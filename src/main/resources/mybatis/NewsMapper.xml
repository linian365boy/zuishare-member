<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="top.zuishare.dao.NewsDao">

    <select id="getIndexNews" resultType="top.zuishare.spi.model.News">
        select id,clicks,title,publishDate,keyWords from tb_news where publishDate is null order by publishDate desc,id desc limit #{limit}
    </select>

</mapper>